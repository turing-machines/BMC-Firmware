config BR2_PACKAGE_ROCKUSB
    bool "Rockusb driver"
    depends on BR2_LINUX_KERNEL
    help
        This is a kernel module that allows block device access to Rockchip
	devices speaking the Rockusb protocol.
